"""Develop a python program to read n digit integer number, and separate the integer number and display each digit. [Hint: input:5678 output: 5 6 7 8, use: floor and mod operators)  """

def separate_digits(number):
    digits = []
    while number > 0:
        digit = number % 10  # Get the last digit using mod
        digits.insert(0, digit)  # Insert the digit at the start of the list
        number = number // 10  # Remove the last digit using floor division
    return digits

# Read an integer from the user
n = int(input("Enter an n-digit integer: "))

# Get the separated digits
digits = separate_digits(n)

# Display each digit separated by a space
#print(" ".join(map(str, digits)))

for digit in digits:
    print(digit,end=" ")
